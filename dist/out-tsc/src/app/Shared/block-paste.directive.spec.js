import { BlockPasteDirective } from './block-paste.directive';
describe('BlockPasteDirective', function () {
    it('should create an instance', function () {
        var directive = new BlockPasteDirective();
        expect(directive).toBeTruthy();
    });
});
//# sourceMappingURL=block-paste.directive.spec.js.map